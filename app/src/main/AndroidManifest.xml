<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hino.dev.dashboardupdater">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.symbol.emdk.permission.EMDK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DashboardUpdater">

        <!-- <uses-library android:name="com.symbol.emdk" /> -->

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sections">

        </activity>
        <activity android:name=".MOList" />
        <activity android:name=".Scan" />
        <activity android:name=".MOPreview" />
        <activity android:name=".MaterialCall" />
        <activity android:name=".SuccessAction" />
        <activity android:name=".ReturnToSection" android:launchMode="singleTop" />
        <activity android:name=".ReviewMO" />
        <activity android:name=".ViewAttachments" />
        <activity android:name=".ShowServerResponse" android:launchMode="singleTop" />
    </application>

</manifest>